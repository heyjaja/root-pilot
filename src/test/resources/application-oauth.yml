spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 248712533217-92ii0d8ed1k6vh2ckbfm7u989mgsgc4f.apps.googleusercontent.com
            client-secret: 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
            scope: profile,email
          naver:
            client-id: udBSmqMiBH6r1GvoMm_D
            client-secret: 6gILXeVcIv
            # yml에서는 '/'를 그대로 그대로 쓰면 파싱 에러 발생 -> ""로 감싸야 한다.
            redirect-uri: "{baseUrl}/{action}/oauth2/code/{registrationId}"
            authorization_grant_type: authorization_code
            scope: name, email, profile_image
            client-name: Naver
        provider:
          naver:
            authorization_uri: https://nid.naver.com/oauth2.0/authorize
            token_uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user_name_attribute: response
            # 네이버 회원 조회시 JSON 반환: 시큐리티에서는 최상위 필드만 명시할 수 있기 때문에 response 지정

# Jwt 관련 설정
header: Authorization
  #HS512 알고리즘을 사용할 것이기 때문에 512bit, 즉 64byte 이상의 secret key를 사용해야 한다.
  #echo 'springboot-pilot-project-root-jwt-secret-secret'|base64
jwt.secret: c3ByaW5nYm9vdC1waWxvdC1wcm9qZWN0LXJvb3Qtand0LXNlY3JldC1zZWNyZXQ=
jwt.token-validity-in-seconds: 86400 #24시간